# ===============================================
# ENVIRONNEMENT DE PRODUCTION - RAILWAY
# ===============================================
# NOTE: Ces variables sont à titre d'exemple
# Les vraies valeurs doivent être configurées dans Railway UI
# ===============================================

# Database (PostgreSQL Railway)
# Railway fournit automatiquement DATABASE_URL
# POSTGRES_DB sera fourni par Railway
# POSTGRES_USER sera fourni par Railway
# POSTGRES_PASSWORD sera fourni par Railway
# DATABASE_URL sera fourni par Railway (format: postgresql://user:pass@host:port/db)

# Redis (Redis Railway)
# REDIS_URL sera fourni par Railway
# REDIS_PASSWORD sera fourni par Railway

# Flask Backend (Production)
SECRET_KEY=${SECRET_KEY}  # À configurer dans Railway
JWT_SECRET_KEY=${JWT_SECRET_KEY}  # À configurer dans Railway
FLASK_APP=run.py
FLASK_ENV=production
FLASK_DEBUG=0
BACKEND_PORT=5000
GUNICORN_WORKERS=4

# Celery (Production)
CELERY_WORKERS=4
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

# Frontend Next.js (Production)
NEXT_PUBLIC_API_URL=https://your-backend.railway.app  # URL Railway Backend
NEXTAUTH_SECRET=${NEXTAUTH_SECRET}  # À configurer dans Railway
NEXTAUTH_URL=https://your-frontend.vercel.app  # URL Vercel Frontend
BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}  # À configurer dans Railway
BETTER_AUTH_URL=https://your-frontend.vercel.app

# Google OAuth (Production)
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}  # À configurer dans Railway
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}  # À configurer dans Railway
GOOGLE_REDIRECT_URI=https://your-frontend.vercel.app/api/auth/callback/google

# Hugging Face
HF_API_TOKEN=${HF_API_TOKEN}  # À configurer dans Railway

# CORS (Production)
CORS_ORIGINS=https://your-frontend.vercel.app

# Mode de production
NODE_ENV=production
